import * as Styled from './styled';

export const Loading = () => {
  return (
    <Styled.Loading>
      <Styled.LoadingFragment />
      <Styled.LoadingFragment />
      <Styled.LoadingFragment />
    </Styled.Loading>
  );
};
